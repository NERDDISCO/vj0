# Run-specific operation evidence

These are the frozen supervisor, shutdown backstop, launch and cache-preservation scripts used for this run. They contain run-specific process IDs, pod addresses and result paths; they are historical evidence, not commands to rerun against a future pod. Final state and verified stop records are added after shutdown. Benchmark calculations use the portable scripts in workers/runpod-flux2klein/bench/.
